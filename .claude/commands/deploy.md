Deploy the project to Google Apps Script using CLASP.

1. Run `npx clasp push` to upload all files in src/ to the linked Apps Script project
2. Report the result and any errors
3. If successful, remind user to refresh their Google Sheet to test changes
