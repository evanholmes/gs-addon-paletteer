Pull the latest code from Google Apps Script editor.

1. Run `npx clasp pull` to download changes made in the online Apps Script editor
2. Show a summary of what files changed
3. Warn if there are local uncommitted changes that might be overwritten
